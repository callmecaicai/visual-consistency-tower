# 阶段 II · 统一 Mask 架构：任何稠密任务一种范式（MaskFormer / Mask2Former / SegFormer / OneFormer / SegGPT / EoMT）

作者 / 机构: Cheng 等 (FAIR/UIUC) / Xie 等 (NVIDIA) / Jain 等 (SHI Labs) / Wang 等 (BAAI) / Kerssies 等 (University of Bonn)
共形维度: 度量, 材料, 载体
关键贡献: 把语义 / 实例 / 全景 / 深度 / 视频对象分割全部抽象成「一组 mask 查询 + 每个查询一个标签」；EoMT 进一步证明 ViT encoder 本身就够——decoder 可选。
年份: 2024
序号: 15
残差 / 它催生了什么: 架构统一了，但类别边界仍锁在训练集之内，第 81 类无法产生 → 催生 SAM / Grounding DINO / APE / CAT-Seg 的提示化与开放词汇。
类型: 架构
阶段: II · 稠密共形的扩张

<aside>
🧭

**章首协议（接回第一原理层）**：本章按“稳定差异 → 闭合半径 → 出口残差”三问读取。

- **稳定差异**：可统一写成 mask 查询的区域差异，以及不同稠密任务共享的分割原语。
- **闭合半径**：把分割、实例、全景、深度等任务压到同一 mask 架构中，显著扩大稠密闭合的任务半径。
- **出口残差**：统一架构仍默认闭集类别表，第 81 类问题暴露出语义来源仍在系统外。

</aside>

<aside>
🧩

**主题**：稠密任务的架构坍缩——语义 / 实例 / 全景 / 深度 / 视频对象分割全部被抽象成「一组 mask 查询 + 每个查询一个标签」。EoMT 甚至证明连专门的 mask decoder 都可以省掉——ViT encoder 本身就够。

</aside>

## 1 · MaskFormer —— *Per-Pixel Classification is Not All You Need for Semantic Segmentation* (Cheng, Schwing, Kirillov, NeurIPS 2021)

**共形贡献**：语义分割不必做「逐像素分类」。改成「预测 N 个 mask + 每个 mask 一个类别」——这和实例分割用的是**同一个公式**。语义 / 实例分割第一次在架构上合流。

🔗 [https://arxiv.org/abs/2107.06278](https://arxiv.org/abs/2107.06278)

## 2 · Mask2Former —— *Masked-attention Mask Transformer for Universal Image Segmentation* (Cheng 等, CVPR 2022)

**共形贡献**：在 MaskFormer 基础上加 masked attention（query 只关注自己当前预测 mask 覆盖的区域）和高效多尺度 decoder。一个配置在语义 / 实例 / 全景三个任务上全部拿 SOTA——架构统一在经验上被证成。

🔗 [https://arxiv.org/abs/2112.01527](https://arxiv.org/abs/2112.01527)

下面把"object query 作为通用接口原语"这个论断打穿。这不是五个案例的并列，而是**一条 query 的本体论升级链**——每一代都在扩展"query 是什么可以被塞进去的东西"。

---

