# §23 · 机制展开：语言如何改写稠密输出的结构

> 本文件是展开占位。定位卡已给出骨架，此处需要阶段 I/II 密度的机制分析。
> 
> **接缝说明**：§16 已把 Grounding DINO / APE / CAT-Seg 等作为"稠密共形的出口节点"引入。本章的视角转换：从"如何打开类别闭集"转为"语言如何改写稠密输出的结构"。这不是重复，是引子 vs 正文。

---

## 待展开清单

### 1 · 代表工作的共形定位卡（逐篇）

需要为 GLIP / Grounding DINO 1.5 / OWL-ViT / OWL-v2 / APE / CAT-Seg / FC-CLIP / SAN 各写标准定位卡。重点是语言注入的机制差异，不是性能数字。

### 2 · "三种语言注入方式"的本体论分析

定位卡§2 列出三种方式但未深入。需要：
- 后端挂载（CLIP 冻结 + 视觉侧空间化）：CAT-Seg / FC-CLIP 的机制
- 前端融合（语言 token 参与 encoder）：GLIP / Grounding DINO 的机制
- 全程渗透（语言作为 query 生成器）：APE / Florence-2 的机制
- 三者在 C5（稠密场共形）和 C6（接口共形）上的不同位置

### 3 · §16→§23 的视角转换论证

需要显式论证：同一批模型（Grounding DINO / APE / Depth Anything）在§16 和§23 中被讨论的维度差异：
- §16 问的是：类别闭集如何被打开？（度量维度）
- §23 问的是：语言如何改写稠密输出的内部结构？（载体维度）

### 4 · 开集检测 vs 开集分割的本体差异

定位卡§6 提到"真假难点"但未展开。需要论证：
- 检测的开集化相对容易（box 是几何原语，语义只在分类头）
- 分割的开集化更难（mask 本身携带语义边界信息）
- 深度估计的开集化为什么走了完全不同的路（Depth Anything 的 DINOv2 蒸馏）
