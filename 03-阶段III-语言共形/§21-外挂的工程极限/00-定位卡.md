# §21 · 外挂的工程极限:融合式 VLM 前史的天花板

作者 / 机构: —
共形维度: 动力, 载体
关键贡献: 把外挂范式(ViLBERT / LXMERT / UNITER / VinVL / OSCAR)的工程极限系统化:预提取 / 冻结 / 弱参与的 detector + BERT 式融合让语言—视觉融合层主要消费二手 region feature,系统上限高度受 detector 前端约束。作为阶段 III 前史,证明三条锁死的瓶颈——detector 封顶、region feature 二手化、bbox+caption 数据扩不动——直接推向 §22 的 CLIP 范式置换。
年份: 2020
序号: 21
残差 / 它催生了什么: 三条出口残差推向 §22:① 视觉塔必须端到端或更深地参与联合训练;② 监督信号必须脱离 bbox(region-word → 整图-整句);③ 数据必须走 web-scale 弱对齐(替代精标)。VinVL 是 region-feature 外挂路线的天花板暴露，而不是对这条路线的简单判死。
类型: 理论/分析
阶段: III · 语言共形的深化

<aside>
🧭

**章首协议（接回第一原理层）**：本章按新版五问与闭合制度读取。

| 问项 | 本章回答 |
|---|---|
| 零度设定 | 视觉区域必须能被语言系统消费。 |
| 无 / 反面 | region feature 是二手视觉：它已经被 detector 类别表、box 制度和 Visual Genome 标注体系解释过一次。 |
| 成为 | ViLBERT、LXMERT、VisualBERT、UNITER、OSCAR、VinVL 把 detector 产物交给 BERT 式融合层。 |
| 闭合制度 | `C_21=(Z_region, Π_VQA/caption/ITM, Γ_box/phrase/region, M_MLM/ITM/WRA, D_detector+BERT-fusion)`。 |
| 成功标准 | 语言能消费 detector 产物，回答 VQA、caption、ITM 与 region-word alignment 任务。 |
| 遮蔽对象 | 原始像素、开放对象、非 box 结构、detector 类别表外的视觉差异。 |
| 内在残差 | detector 上限锁死语义，直接逼出 §22 的 web-scale 图文对齐。 |

</aside>

<aside>
🧩

**主题**:阶段 III §21 的前史章。外挂范式把预训练 detector + BERT 式融合拼在一起,视觉前端通常以预提取 region feature 的形式进入 VLM,没有和语言模型一起充分端到端更新——这条路被 detector 前端的上限锁死,给 §22 的 CLIP 范式置换铺路。

</aside>

## §0 · 本体命题

> 把语言「接」到视觉上,不能让视觉真正承担语义——外挂范式的上限就是 detector 前端的上限。
> 

§21 不是阶段 III 的真正出生，而是阶段 II 对象制度在语言系统中的第一次试用。它证明语言可以消费对象区域，但也证明二手视觉不能承载开放公共语义。

## §1 · 承接点

从阶段 I §5→§10 的残差接入:判别式语义、类别闭集。Faster R-CNN 这份遗产直接成为本章的反面教材——它作为检测器很成功,作为 VL 的视觉前端却把整个范式锁死。

## §2 · 代表工作的谱系

- **ViLBERT**(2019,Lu et al.):双流 cross-attention,首次把 BERT 搬进 VL
- **LXMERT**(2019,Tan & Bansal):三编码器(L / X / V)+ 多任务 pretrain
- **VisualBERT**(2019,Li et al.):单流,最简洁的拼接
- **UNITER**(2020,Chen et al.):统一融合 + 更丰富的预训练目标(WRA / MRM / MRFR)
- **OSCAR**(2020,Li et al.):把 object tag 作为「锚点词」插进 caption——外挂范式里最聪明的数据技巧
- **VinVL**(2021,Zhang et al.):更强的 detector(ResNeXt-152 + 属性头)把这条路推到极限

## §3 · 统一结构(不是「架构差异」,是同一个范式)

- **视觉侧**:冻结的 Faster R-CNN 抽 region features(36 × 2048)
- **文本侧**:BERT-style Transformer
- **融合**:双流 cross-attention 或单流 self-attention
- **训练目标**:MLM + ITM(image-text matching)+ region-word alignment

所有外挂 VLM 都可以映射到这个结构,它们的差异只是「怎么融合」和「加什么目标」,而不是「视觉如何表达语义」。

## §4 · 包装层 vs 工程层

- **包装**:多模态 Transformer / 跨模态注意力 / 统一 VL 表征
- **工程现实**:视觉前端作为已经训练好的 detector 被预提取、冻结或弱参与。融合层学会的是「**如何消费二手 region feature**」,而不是如何从像素中重新组织语义。视觉的能力上限因此高度受 detector 的类别表、box 制度和 Visual Genome 标注体系约束。

## §5 · 三条锁死的瓶颈

1. **Detector 封顶**:VG 1600 类是封闭集,bbox 粒度是预设的,类别无法扩展
2. **Region feature 是二手视觉**:已经被 detector 语义化过一次,丢掉了原始像素的大量信息
3. **数据扩不动**:需要 bbox + caption 双重标注,扩到 10⁷ 就是极限

## §6 · VinVL 暴露这条路的天花板

VinVL 把 region-feature 外挂路线推到很高位置:更强 backbone、属性头、更细的对象 tag 和更多预训练目标都在加固 detector 前端。但它暴露的不是“错误路线已经完结”,而是这条路线的边际收益递减:继续增强 detector 和 region 监督,会被数据规模与端到端图文对齐范式甩开。VinVL 与 CLIP 不完全处在同一 benchmark 制度下,因此不能写成简单输赢;它更准确地说明外挂范式的扩张半径已经接近天花板。

## §7 · 出口残差 → §22

- 视觉塔必须**端到端参与训练**(不能只做特征提取)
- 监督信号必须**脱离 bbox**(region-word → 整图-整句)
- 数据扩张必须**抛弃精标**(精标 → web-scale 弱对齐)

这三条残差共同推向 §22 的 CLIP——**监督经济学的范式置换**。

## §8 · 一句话收束

> 外挂范式不是错的,是**被规模和结构双重甩开**:它的工程天花板正好是 CLIP 的起点。
>
