# §24 · 机制展开：统一多模态骨干的收敛与代价

> 本文件是展开占位。定位卡已给出骨架，此处需要阶段 I/II 密度的机制分析。

---

## 待展开清单

### 1 · 代表工作的共形定位卡（逐篇）

需要为 CoCa / BLIP / BLIP-2 / Florence-2 / PaLI / PaLI-X / OFA / Unified-IO / Unified-IO 2 各写标准定位卡。

### 2 · "两种统一哲学"的机制对比

定位卡§2 提到两种哲学但未展开。需要：
- 哲学A（共享 encoder + 多任务头）：CoCa / PaLI 路线
- 哲学B（桥接模块 + 冻结 LLM）：BLIP-2 / Flamingo 路线
- 两者在参数效率、任务泛化、训练稳定性上的 trade-off
- 为什么哲学B最终胜出（BLIP-2 的 Q-Former 成为后续标准）

### 3 · BLIP-2 的 Q-Former 为什么是关键转折

定位卡§4 断言 BLIP-2 是关键但未论证。需要：
- Q-Former 的机制：可学习 query 从冻结视觉 encoder 提取信息 → 喂给冻结 LLM
- 这个设计为什么同时解决了训练效率和模态对齐
- Q-Former 与 DETR 的 object query 的结构同构性
- 后续工作（InstructBLIP / Qwen-VL / InternVL）如何继承或抛弃 Q-Former

### 4 · Florence-2 的 prompt-as-task 范式

定位卡§5 提到但未展开。需要：
- `<loc_*>` token 的设计：把所有任务序列化为文本
- 这与 §15 的 query-based 统一和 §16 的 prompt-based 统一的关系
- Florence-2 的 FLD-5B 数据引擎与 SAM 的 SA-1B 数据引擎的结构同构
