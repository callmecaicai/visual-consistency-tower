# §24 · 统一多模态骨干:对比 / caption / VQA / grounding 归一化

作者 / 机构: —
共形维度: 动力, 载体
关键贡献: 吃透 VLM 的第二根柱子。统一骨干两种哲学(共享编码器+多头 vs Seq2Seq 语言建模)的深层差异,Seq2Seq 胜出并通往 §25。关键转折:BLIP-2 的 Q-Former 首次让视觉和 LLM 解耦——是通往 VLM 顶点的桥。Florence-2 的 prompt-as-task 是统一骨干的终极形态。穿透工程代价(统一骨干常常打不过专用模型,真实价值是为 LLM 提供标准视觉接口)。
年份: 2022
序号: 24
残差 / 它催生了什么: 两条出口残差:① 语义能力被自身 text decoder 封顶 → 必须换成 LLM → §25;② 生成能力缺席 → 阶段 IV。
类型: 理论/分析
阶段: III · 语言共形的深化

<aside>
🧭

**章首协议（接回第一原理层）**：本章按“稳定差异 → 闭合半径 → 出口残差”三问读取。

- **稳定差异**：可同时支撑对比、caption、VQA 与 grounding 的统一多模态差异。
- **闭合半径**：把语言从外挂目标推进为共同骨干，使多任务共享的视觉—语言接口开始稳定化。
- **出口残差**：统一骨干仍被自带 decoder 封顶，视觉生成能力也未闭合，系统最终要把语义后端交给 LLM 或生成模型。

</aside>

<aside>
🏛️

**主题**:阶段 III §24。当一个骨干能同时承担对比对齐、caption、VQA、grounding 时,语言不再是视觉的下游任务——它成了视觉的共同语法。这一节是 VLM 从「拼接」走向「骨架」的关键过渡。

</aside>

## §0 · 本体命题

> 统一骨干的意义不是「一个模型打多个 benchmark」,是**承认语言是所有视觉理解任务的公共基底**——这一步让视觉塔可以进一步退化为 adapter,为 §25 的 LLM 后端铺路。
> 

## §1 · 承接点

- §22 CLIP 提供对齐目标(单目标)
- §23 语言锚定稠密提供空间 / 类别目标(多目标)
- 本节把所有目标塞进同一个骨干

## §2 · 统一骨干的两种哲学

| 哲学 | 结构 | 代表 |
| --- | --- | --- |
| Seq2Seq 语言建模 | 所有输出都是 token 序列 | OFA / Unified-IO / Florence-2 / PaLI |

深层差异:**第一种把视觉和语言编码器对称对待;第二种把一切翻译成语言**。第二种是阶段 III 胜出的范式——因为它通往 §25 的 LLM。

## §3 · 代表工作

- **CoCa**(2022,Yu et al.):对比 + captioning 双目标,视觉和语言解码器共享
- **BLIP**(2022):三目标(ITC / ITM / LM),CapFilt 数据清洗
- **BLIP-2**(2023):**Q-Former 首次让视觉和 LLM 解耦**——这是通往 §25 的桥
- **OFA**(2022):一切皆 seq2seq,任务用自然语言 prompt 描述
- **Unified-IO / Unified-IO 2**:进一步统一到生成任务(图像、音频)
- **PaLI / PaLI-3**(Google):多语言 + 统一训练
- **Florence-2**(2024,Microsoft):**prompt-as-task**——用 task token 替代 task head

## §4 · BLIP-2 为什么是关键转折

BLIP-2 的 **Q-Former**(Querying Transformer):

- 输入:冻结的视觉特征 + 32 个可学习 query token
- 输出:32 个压缩后的视觉 token
- 意义:**首次让视觉塔和 LLM 完全解耦**——视觉塔只负责抽特征,Q-Former 负责转成 LLM 能吃的格式,LLM 冻结

这个结构直接预言了 §25 的全部范式。后来 LLaVA 用「2 层 MLP」替代 Q-Former,证明 **Q-Former 可以更简单**,但 BLIP-2 的解耦思想是革命性的。

## §5 · Florence-2 的 prompt-as-task

Florence-2 用一个统一的 seq2seq 模型做:

- caption / detailed caption / OD / OCR / grounding / segmentation
- 所有任务用自然语言 prompt 触发(如 `<OD>`、`<CAPTION>`)
- task token 替代 task head

意义:**任务本身成为 prompt**,这是统一骨干的终极形态,也是通往阶段 IV「指令即任务」的桥。

## §6 · 工程代价(被低估的部分)

- 统一骨干在单任务上**常常打不过专用模型**
- 「统一」的价值不是 SOTA,是**下一步借 LLM** 时的接口一致性
- 训练成本高(多任务 loss 平衡、数据配比、grad 冲突)
- **多数产业应用仍用专用模型**,统一骨干主要活在研究和旗舰 VLM 里

## §7 · 包装 vs 工程

- **包装**:「统一多模态理解」「单模型多任务」
- **工程**:真正的价值在于**为 LLM 提供标准视觉接口**——BLIP-2 的 Q-Former 的历史地位远高于它的 benchmark 分数

## §8 · 出口残差

- 统一骨干的语义能力仍被**自身 text decoder 封顶**
- 所以必须把 text decoder 换成 LLM → §25
- 生成能力仍缺席(caption ≠ 生成)→ 阶段 IV

## §9 · 一句话收束

> 统一骨干是 VLM 的过渡骨架——它的历史任务是**铺好 LLM 能够插入视觉的标准接口**,然后光荣退位。
>