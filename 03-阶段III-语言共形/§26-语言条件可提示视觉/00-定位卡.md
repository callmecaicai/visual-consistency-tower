# §26 · 语言条件可提示视觉:从理解到像素级输出

作者 / 机构: —
共形维度: 主体性, 动力
关键贡献: 语言指令 → 像素级输出的闭环建立。三种范式(外部组装 / 坐标作为 token / embedding-as-mask)本体差异,LISA 的 SEG 特殊 token + mask decoder 是端到端语言→mask 闭合的代表性胜利者(可微、可推理、可组合),但 Grounded-SAM 式 pipeline 在工程灵活性和快速部署上仍有优势。三类任务(referring / reasoning segmentation / grounded conversation)对语言能力的要求递增。穿透叙事泡沫:真实使用中的 referring 失败率极高(歧义、多对象、关系词、组合指令)。
年份: 2023
序号: 26
残差 / 它催生了什么: 两条出口残差:① 语言可定位/选择,但不能合成新像素 → 阶段 IV;② 空间推理仍靠语言先验,不是视觉推理,具身任务暴露这一点 → 阶段 V。
类型: 理论/分析
阶段: III · 语言共形的深化

<aside>
🧭

**章首协议（接回第一原理层）**：本章按“稳定差异 → 闭合半径 → 出口残差”三问读取。

- **稳定差异**：可被语言指令稳定调用的区域、对象与像素级输出差异。
- **闭合半径**：把语言共形从“能描述”推进到“能输出空间结果”，建立语言到像素级输出的单向闭环。
- **出口残差**：操作不等于生成，空间推理仍主要借助语言先验；一旦进入真实世界状态与新观测生成，这条闭合就不够。

</aside>

<aside>
🎛️

**主题**:阶段 III §26。语言指令 → 像素级输出的闭环建立——视觉从「被描述」走向「被空间化调用」。referring / reasoning segmentation / grounded conversation 三类任务的统一本体。

</aside>

## §0 · 本体命题

> 语言可以定位像素,也可以触发像素级输出——视觉模型第一次具备「**听懂复杂指令、给出像素级输出**」的闭环能力。
> 

## §1 · 承接点

- §23 语言锚定稠密提供**单步**定位能力
- §25 VLM 提供**多轮推理和指令理解**能力
- 本节把两者合成:指令驱动的像素级输出

## §2 · 三种范式(本体差异巨大)

| 范式 | 机制 | 代表 | 本体 |
| --- | --- | --- | --- |
| 外部组装 | 多模型串联 | Grounded-SAM | 工程拼装 |
| 坐标作为 token | bbox / region 编码成语言 | Shikra / Ferret / KOSMOS-2 | 语言即坐标 |
| embedding-as-mask | SEG token → mask decoder | LISA / GLaMM / PixelLM | 语言触发 mask |

**条件性判断**:embedding-as-mask 是“端到端语言→mask 闭合”的代表性胜利者,因为它可微、可推理、可组合;但 Grounded-SAM 式 pipeline 在工程灵活性、模型可替换、开放词汇组合和快速部署上仍有优势。

## §3 · 代表工作的谱系

- **Grounded-SAM**(2023):GroundingDINO + SAM 串联,工程最快
- **SEEM**(2023):统一 prompt(text / click / box / mask)
- **Semantic-SAM**(2023):SAM 的语义扩展
- **KOSMOS-2**(2023,Microsoft):把 bbox 作为语言 token
- **Shikra**(2023):坐标直接写在文本里
- **Ferret / Ferret-v2**(2024,Apple):任意形状 region 作为 prompt
- **LISA**(2023,港中文):**`<SEG>` token + mask decoder**,reasoning segmentation 的开创者
- **LISA++ / GLaMM / PixelLM**:LISA 路线的扩展
- **VisionLLM**:更激进的统一视觉语言模型

## §4 · LISA 为什么是端到端闭合代表

LISA 的关键设计:

- LLM 输出一个特殊的 `<SEG>` token
- 这个 token 的 embedding 被送进 SAM 的 mask decoder
- mask decoder 产生像素级 mask

意义:

- **mask 是隐变量**,不是解码出来的字符串
- 端到端可微,推理链完整
- 支持复杂指令(「找到可以用来打开罐头的物体」,不是简单 referring)

这个结构直接影响了后续许多 pixel-level VLM,但它不是总体胜利者。它的地位是:在 reasoning segmentation 中最清楚地打通“语言推理 -> mask 输出”。

## §5 · 三类任务的本体差异

| 任务 | 输入 | 输出 | 关键能力 |
| --- | --- | --- | --- |
| referring expression | 「左边那只红色的狗」 | bbox / mask | 空间 + 属性定位 |
| reasoning segmentation | 「可以坐的东西」 | mask | 常识推理 |
| grounded conversation | 多轮对话 + 指代 | mask + 文本 | 长程 context |

三类任务看起来相似,但**对语言能力的要求递增**:referring 主要考视觉,reasoning segmentation 考常识,grounded conversation 考 context maintenance。

## §6 · 工程现实与叙事泡沫

- **叙事**:「用自然语言精确控制像素」
- **工程**:在 benchmark 上看起来很强,但**真实使用失败率极高**
    - 歧义(「那只」指哪只?)
    - 多对象(「所有」「最大的」容易漏)
    - 关系词失效(「在...旁边」「在...之间」)
    - 组合指令(多个约束联合)很脆

## §7 · 为什么这条路仍未真正闭环

- 语言可以**定位**和**选择**,但**不能合成新像素**
- 没有真正的「视觉动作空间」——能做的只是「选择现有像素」
- 合成(画出没有的东西、编辑、延续)必须交给阶段 IV 的 diffusion

## §8 · 出口残差

- **残差 A → 阶段 IV**:视觉不能反向合成(画出 / 编辑 / 延续)
- **残差 B → 阶段 V**:空间推理仍严重依赖语言先验,不是视觉推理——机器人 / 具身任务暴露这一点

## §9 · 一句话收束

> 语言条件可提示视觉让视觉第一次「听话」——但**听话不等于持有世界**,阶段 III 在这里把指令-感知-空间输出的闭环建立好,却把指令-生成的闭环留给阶段 IV。
>
