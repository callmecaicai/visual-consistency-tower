# §25 · 机制展开：LLM 作为语义后端——VLM 的三代演进

> 本文件是展开占位。定位卡已给出骨架，此处需要阶段 I/II 密度的机制分析。

---

## 待展开清单

### 1 · 三代 VLM 的共形定位卡（逐篇）

定位卡§2 给了三代分类但每代只有列表。需要为关键节点写标准定位卡：
- 第一代桥式：Frozen / Flamingo / KOSMOS-1 / LLaVA v1
- 第二代简化：LLaVA-1.5 / InstructBLIP / Qwen-VL / CogVLM
- 第三代生产级：InternVL 2.5 / Qwen2.5-VL / Cambrian-1
- 反思类：Cambrian-1 的"视觉仍是瓶颈"论证

### 2 · 三种视觉-LLM 接口的机制对比

定位卡§3 列出三种接口但未展开。需要：
- 线性投影（LLaVA）：最简但信息压缩最粗暴
- Q-Former / Perceiver Resampler（BLIP-2 / Flamingo）：可学习压缩
- 动态分辨率 token 化（Qwen2.5-VL / InternVL）：保留空间信息
- 三者在视觉信息保真度和 LLM 上下文占用之间的 trade-off 曲线

### 3 · "LLM 是瓶颈，不是视觉塔"的论证

定位卡§5 断言但未给证据链。需要：
- Cambrian-1 的实验：换视觉 encoder 对最终性能的影响 < 换 LLM
- 这意味着什么：视觉信息在进入 LLM 之前已经被压缩到 LLM 能处理的带宽
- 与§27 的"借语言的代价"的关系

### 4 · 视觉 token 的工程战争

定位卡§6 提到但未展开。需要：
- token 数量 vs 性能的 scaling 曲线
- 动态分辨率策略（tile / anyres / native resolution）的机制对比
- 为什么视觉 token 压缩是 VLM 工程的核心战场
